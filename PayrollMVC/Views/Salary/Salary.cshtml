@model IEnumerable<PayrollMVC.Models.Salary>

@{
    ViewBag.Title = "Salary";
}

<h2>Salary</h2>

<p>
    @Html.ActionLink("Pay Salary ", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Salary1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SalryDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Salary1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SalryDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
